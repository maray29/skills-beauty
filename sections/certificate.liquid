{{ 'certificate.css' | asset_url | stylesheet_tag }}

<div class="page-width padding-section-large">
  <div class="layout_12col">
    <div class="section_heading-wrap">
      <h2>{{ section.settings.heading }}</h2>
    </div>
    <div class="section_subheading-wrap">
      {% render 'tag-component', tag_content: section.settings.tag_content %}
      {% comment %}
        {%- for block in section.blocks -%}
        {%- endfor -%}
      {% endcomment %}
      <p class="subtitle">Цифровой сертификат подходит для оплаты услуг, курсов и продукции в магазине.</p>
    </div>
    <div class="certificate_img-wrap">
      {%- if section.settings.image != blank -%}
        {{
          section.settings.image
          | image_url: width: 1500
          | image_tag: sizes: sizes, widths: widths, fetchpriority: fetch_priority
        }}
      {%- else -%}
        {{ 'detailed-apparel-1' | placeholder_svg_tag: 'placeholder-svg' }}
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:sections.certificate.name",
  "tag": "section",
  "class": "section_certificate",
  "settings": [
    {
      "type": "inline_richtext",
      "id": "heading",
      "default": "Подарочный Сертификат",
      "label": "t:sections.team.settings.heading.label"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:sections.certificate.settings.image.label"
    },
    {
      "type": "text",
      "id": "tag_content",
      "default": "Наши задачи и цели",
      "label": "Tag Content"
    }
  ],
  "presets": [
    {
      "name": "t:sections.cerificate.presets.name"
    }
  ]
}
{% endschema %}
